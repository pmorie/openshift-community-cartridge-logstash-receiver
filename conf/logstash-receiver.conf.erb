input {
redis { host => "<%= ENV['OPENSHIFT_REDIS_HOST']%>"
        port => "<%= ENV['OPENSHIFT_REDIS_PORT']%>"
        password => "<%= ENV['OPENSHIFT_REDIS_PASSWORD']%>"
        data_type => "list"
        key => "osedata"  }
}
output {
  elasticsearch_http { host => "<%= ENV['OPENSHIFT_ELASTICSEARCH_HOST']%>"
                       port => "<%= ENV['OPENSHIFT_ELASTICSEARCH_PORT']%>" }
}
